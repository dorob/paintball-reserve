<% if (loggedIn) { %>
  <% include logged_in_header %>
<% } else { %>
  <% include logged_out_header %>
<% } %>

  <% if (reserve_succeed) { %>
    <% include reserve_succeed %>
  <% } %>
  <% if (registration_needed) { %>
    <% include registration_needed %>
  <% } %>
  <% if (home_content) { %>
    <% include home_content %>
  <% } %>

<% include footer %>
